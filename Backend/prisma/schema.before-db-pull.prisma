generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
}

// ======================================================
// ADMIN
// ======================================================

model Admin {
  adminId      Int      @id @default(autoincrement()) @map("admin_id")
  adminName    String   @map("admin_name") @db.VarChar(100)
  email        String   @unique @db.VarChar(150)
  phone        String?  @db.VarChar(20)
  passwordHash String   @map("password_hash") @db.Text
  companyId    Int
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  company          companies        @relation(fields: [companyId], references: [company_id])
  approvedAdvances AdvancePayment[] @relation("ApprovedAdvances")

  @@index([companyId])
  @@map("admin")
}

// ======================================================
// BRANCH
// ======================================================

model Branch {
  branchId   Int      @id @default(autoincrement()) @map("branch_id")
  branchName String   @map("branch_name") @db.VarChar(100)
  location   String   @db.VarChar(150)
  companyId  Int
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")

  company        companies       @relation(fields: [companyId], references: [company_id])
  departments    Department[]
  employees      Employee[]
  devices        IotDevice[]
  publicHolidays PublicHoliday[]

  @@index([companyId])
  @@map("branches")
}

// ======================================================
// DEPARTMENT
// ======================================================

model Department {
  departmentId   Int      @id @default(autoincrement()) @map("department_id")
  departmentName String   @map("department_name") @db.VarChar(100)
  branchId       Int
  companyId      Int
  managerId      Int?
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")

  branch    Branch     @relation(fields: [branchId], references: [branchId])
  company   companies  @relation(fields: [companyId], references: [company_id])
  manager   Employee?  @relation("DepartmentManager", fields: [managerId], references: [employeeId])
  employees Employee[] @relation("EmployeeDepartment")

  @@index([branchId])
  @@index([companyId])
  @@index([managerId])
  @@map("departments")
}

// ======================================================
// EMPLOYEE
// ======================================================

model Employee {
  employeeId Int       @id @default(autoincrement()) @map("employee_id")
  firstName  String    @map("first_name") @db.VarChar(100)
  lastName   String    @map("last_name") @db.VarChar(100)
  gender     String?   @db.VarChar(20)
  email      String    @unique @db.VarChar(150)
  phone      String?   @db.VarChar(20)
  hireDate   DateTime? @map("hire_date") @db.Date
  role       String    @db.VarChar(30)

  // Monthly/base salary
  baseSalary Decimal? @map("base_salary") @db.Decimal(12, 2)

  // Expected working hours per month
  monthlyExpectedHours Decimal? @map("monthly_expected_hours") @db.Decimal(8, 2)

  // Calculated hourly rate
  // baseSalary / monthlyExpectedHours
  salaryRatePerHour Decimal? @map("salary_rate_per_hour") @db.Decimal(10, 2)

  companyId    Int
  branchId     Int
  departmentId Int?
  managerId    Int?
  status       String   @db.VarChar(20)
  passwordHash String   @map("password_hash") @db.Text
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  activityLogs           ActivityLog[]
  advancePayments        AdvancePayment[]        @relation("EmployeeAdvances")
  attendance             Attendance[]
  attendancePunches      AttendancePunch[]
  managedDepartments     Department[]            @relation("DepartmentManager")
  fingerprintEnrollments FingerprintEnrollment[]
  fingerprintTemplates   FingerprintTemplate[]
  leaveBalances          LeaveBalance[]
  leaveRequests          LeaveRequest[]
  payrolls               Payroll[]

  // Extra-work accumulation and settlement history
  extraWorkRecords     ExtraWork[]
  extraWorkSettlements ExtraWorkSettlement[]

  branch       Branch      @relation(fields: [branchId], references: [branchId])
  company      companies   @relation(fields: [companyId], references: [company_id])
  department   Department? @relation("EmployeeDepartment", fields: [departmentId], references: [departmentId])
  manager      Employee?   @relation("EmployeeManager", fields: [managerId], references: [employeeId])
  subordinates Employee[]  @relation("EmployeeManager")

  @@index([companyId])
  @@index([branchId])
  @@index([departmentId])
  @@index([managerId])
  @@map("employees")
}

// ======================================================
// IOT DEVICE
// ======================================================

model IotDevice {
  deviceId         Int       @id @default(autoincrement()) @map("device_id")
  deviceCode       String    @unique @map("device_code") @db.VarChar(50)
  deviceName       String    @map("device_name") @db.VarChar(100)
  branchId         Int
  companyId        Int
  location         String?   @db.VarChar(150)
  status           String    @db.VarChar(20)
  lastSeenAt       DateTime? @map("last_seen_at")
  createdAt        DateTime  @default(now()) @map("created_at")
  updatedAt        DateTime  @updatedAt @map("updated_at")
  deviceSecretHash String?   @map("device_secret_hash") @db.VarChar(255)

  attendancePunches AttendancePunch[]
  deviceLogs        DeviceLog[]

  branch  Branch    @relation(fields: [branchId], references: [branchId])
  company companies @relation(fields: [companyId], references: [company_id])

  @@index([branchId])
  @@index([companyId])
  @@map("iot_devices")
}

// ======================================================
// FINGERPRINT TEMPLATE
// ======================================================

model FingerprintTemplate {
  templateId Int      @id @default(autoincrement()) @map("template_id")
  employeeId Int
  sensorSlot Int      @unique @map("sensor_slot")
  fingerName String?  @map("finger_name") @db.VarChar(30)
  status     String   @db.VarChar(20)
  enrolledAt DateTime @map("enrolled_at")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")

  employee Employee @relation(fields: [employeeId], references: [employeeId])

  @@index([employeeId])
  @@map("fingerprint_templates")
}

// ======================================================
// ATTENDANCE PUNCH
// ======================================================

model AttendancePunch {
  punchId    BigInt   @id @default(autoincrement()) @map("punch_id")
  employeeId Int
  deviceId   Int
  sensorSlot Int      @map("sensor_slot")
  punchType  String   @map("punch_type") @db.VarChar(10)
  punchedAt  DateTime @map("punched_at")
  createdAt  DateTime @default(now()) @map("created_at")

  device   IotDevice @relation(fields: [deviceId], references: [deviceId])
  employee Employee  @relation(fields: [employeeId], references: [employeeId])

  @@index([employeeId])
  @@index([deviceId])
  @@index([sensorSlot])
  @@index([punchedAt])
  @@map("attendance_punches")
}

// ======================================================
// ATTENDANCE
// ======================================================

model Attendance {
  attendanceId Int       @id @default(autoincrement()) @map("attendance_id")
  employeeId   Int
  date         DateTime  @db.Date
  checkInTime  DateTime? @map("check_in_time") @db.Time(0)
  checkOutTime DateTime? @map("check_out_time") @db.Time(0)
  totalHours   Decimal?  @map("total_hours") @db.Decimal(5, 2)
  status       String    @db.VarChar(20)
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @updatedAt @map("updated_at")

  employee Employee @relation(fields: [employeeId], references: [employeeId])

  @@unique([employeeId, date])
  @@index([employeeId])
  @@index([date])
  @@map("attendance")
}

// ======================================================
// DEVICE LOG
// ======================================================

model DeviceLog {
  logId     BigInt   @id @default(autoincrement()) @map("log_id")
  deviceId  Int
  eventType String   @map("event_type") @db.VarChar(50)
  message   String?  @db.Text
  createdAt DateTime @default(now()) @map("created_at")

  device IotDevice @relation(fields: [deviceId], references: [deviceId])

  @@index([deviceId])
  @@index([eventType])
  @@index([createdAt])
  @@map("device_logs")
}

// ======================================================
// ADVANCE PAYMENT
// ======================================================

model AdvancePayment {
  advanceId  Int @id @default(autoincrement()) @map("advance_id")
  employeeId Int

  // Original amount requested by employee
  amount Decimal @db.Decimal(10, 2)

  // Amount approved by admin
  approvedAmount Decimal? @map("approved_amount") @db.Decimal(10, 2)

  // Actual amount paid by admin
  paidAmount Decimal? @map("paid_amount") @db.Decimal(10, 2)

  reason      String?  @db.Text
  paymentDate DateTime @map("payment_date") @db.Date

  approvedBy Int?
  status     String @db.VarChar(20)

  createdAt  DateTime  @default(now()) @map("created_at")
  updatedAt  DateTime  @updatedAt @map("updated_at")
  deductedAt DateTime? @map("deducted_at")

  // Payroll that deducted this advance
  deductedInPayrollId Int? @unique @map("deducted_in_payroll_id")

  approver          Admin?   @relation("ApprovedAdvances", fields: [approvedBy], references: [adminId])
  deductedInPayroll Payroll? @relation("AdvancePayroll", fields: [deductedInPayrollId], references: [payrollId])
  employee          Employee @relation("EmployeeAdvances", fields: [employeeId], references: [employeeId])

  @@index([employeeId])
  @@index([approvedBy])
  @@map("advance_payments")
}

// ======================================================
// PAYROLL
// ======================================================

model Payroll {
  payrollId  Int @id @default(autoincrement()) @map("payroll_id")
  employeeId Int

  payPeriodStart DateTime @map("pay_period_start") @db.Date
  payPeriodEnd   DateTime @map("pay_period_end") @db.Date

  // ====================================================
  // HISTORICAL SALARY SNAPSHOT
  // ====================================================

  baseSalary           Decimal? @map("base_salary") @db.Decimal(12, 2)
  monthlyExpectedHours Decimal? @map("monthly_expected_hours") @db.Decimal(8, 2)
  salaryRatePerHour    Decimal? @map("salary_rate_per_hour") @db.Decimal(10, 2)

  // ====================================================
  // ATTENDANCE CALCULATION
  // ====================================================

  // Total actual attendance hours in this payroll period.
  //
  // These are actual fingerprint/attendance hours only.
  totalWorkingHours Decimal @map("total_working_hours") @db.Decimal(8, 2)

  // Hours that are payable at the normal salary rate.
  // Cannot exceed monthlyExpectedHours.
  regularWorkingHours Decimal @default(0.00) @map("regular_working_hours") @db.Decimal(8, 2)

  // Hours below the expected monthly hours.
  shortageHours Decimal @default(0.00) @map("shortage_hours") @db.Decimal(8, 2)

  // Monetary deduction caused by shortage hours.
  shortageDeduction Decimal @default(0.00) @map("shortage_deduction") @db.Decimal(12, 2)

  // Hours above the expected monthly hours.
  // These are stored separately and accumulated across payroll periods.
  extraHours Decimal @default(0.00) @map("extra_hours") @db.Decimal(8, 2)

  // ====================================================
  // PUBLIC HOLIDAY HOURS
  // ====================================================

  // Total paid public-holiday hours included in this
  // payroll period.
  //
  // These are NOT fingerprint attendance hours.
  //
  // They represent paid public-holiday entitlement for
  // the employee's branch.
  paidHolidayHours Decimal @default(0.00) @map("paid_holiday_hours") @db.Decimal(8, 2)

  // ====================================================
  // SALARY
  // ====================================================

  // Regular salary after applying shortage calculation.
  basicSalary Decimal @map("basic_salary") @db.Decimal(12, 2)

  // Incentive paid when accumulated extra work is settled.
  //
  // This field is retained for payroll compatibility/history.
  // Independent variable payments are recorded in
  // ExtraWorkSettlement.
  incentiveAmount Decimal @default(0.00) @map("incentive_amount") @db.Decimal(12, 2)

  // Total advance deduction for the finalized payroll
  advanceDeduction Decimal @default(0.00) @map("advance_deduction") @db.Decimal(12, 2)

  // Final amount payable
  netSalary Decimal @map("net_salary") @db.Decimal(12, 2)

  // ====================================================
  // PAYMENT
  // ====================================================

  // Date from which admin is allowed to pay this payroll.
  //
  // This applies ONLY to normal salary payroll payment.
  scheduledPaymentDate DateTime? @map("scheduled_payment_date") @db.Date

  // Actual date on which admin completed the normal payroll payment.
  paymentDate DateTime? @map("payment_date") @db.Date

  // UNPAID -> PAID
  status String @default("UNPAID") @db.VarChar(20)

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // ====================================================
  // RELATIONS
  // ====================================================

  advanceDeductionRecord AdvancePayment? @relation("AdvancePayroll")

  employee Employee @relation(fields: [employeeId], references: [employeeId])

  // ----------------------------------------------------
  // EXTRA WORK
  // ----------------------------------------------------

  // A payroll can have multiple ExtraWork records.
  //
  // This is intentional because an employee can:
  //
  //   accumulate overtime
  //       ->
  //   settle it
  //       ->
  //   accumulate overtime again
  //       ->
  //   settle it again
  //
  // Historical ExtraWork records are preserved.

  extraWorkRecords ExtraWork[]

  // ----------------------------------------------------
  // EXTRA WORK SETTLEMENTS
  // ----------------------------------------------------

  // A payroll may have zero or more historical settlement
  // references.
  //
  // Variable/overtime payment does NOT require normal
  // payroll payment.

  extraWorkSettlements ExtraWorkSettlement[]

  @@index([employeeId])
  @@index([payPeriodStart, payPeriodEnd])
  @@index([status])
  @@index([scheduledPaymentDate])
  @@map("payroll")
}

// ======================================================
// EXTRA WORK
// ======================================================
//
// Stores extra hours generated by payroll periods.
//
// Example:
//
// September -> 25 hours
// October   -> 15 hours
//
// Current accumulated balance = 40 hours.
//
// These records are NOT deleted when an incentive is paid.
// They are linked to a settlement for historical tracking.
//
// Lifecycle:
//
// ACCUMULATED -> SETTLED
//
// ACCUMULATED -> REJECTED
//
// A new accumulation record can be created after a previous
// record has been settled.
// ======================================================

model ExtraWork {
  extraWorkId Int @id @default(autoincrement()) @map("extra_work_id")

  employeeId Int

  // ----------------------------------------------------
  // PAYROLL SOURCE
  // ----------------------------------------------------

  // The payroll period that generated these extra hours.
  //
  // IMPORTANT:
  // This field is intentionally NOT unique.
  //
  // The same payroll period can have multiple ExtraWork
  // accumulation records over time.

  payrollId Int @map("payroll_id")

  // Number of extra hours generated during this
  // accumulation cycle.
  extraHours Decimal @default(0.00) @map("extra_hours") @db.Decimal(8, 2)

  // ACCUMULATED -> SETTLED
  // ACCUMULATED -> REJECTED
  status String @default("ACCUMULATED") @db.VarChar(20)

  // NULL while these hours are still accumulated.
  //
  // Set when the hours are included in an
  // ExtraWorkSettlement.
  settlementId Int? @map("settlement_id")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt

  employee Employee @relation(fields: [employeeId], references: [employeeId])

  payroll Payroll @relation(fields: [payrollId], references: [payrollId])

  settlement ExtraWorkSettlement? @relation(fields: [settlementId], references: [settlementId])

  @@index([employeeId])
  @@index([payrollId])
  @@index([status])
  @@index([settlementId])
  @@index([createdAt])
  @@map("extra_work")
}

// ======================================================
// EXTRA WORK SETTLEMENT
// ======================================================
//
// Historical record of a variable/overtime payment.
//
// Example:
//
// September extra = 25
// October   extra = 15
// -----------------
// Settled hours   = 40
// Incentive       = ₹2,000
//
// The settlement record remains permanently.
//
// The ExtraWork records are marked SETTLED.
//
// A new accumulation cycle can then begin.
//
// IMPORTANT:
//
// This settlement is independent from normal payroll
// payment.
//
// Therefore payrollId is OPTIONAL.
//
// A variable payment can happen before the normal payroll
// scheduled payment date.
// ======================================================

model ExtraWorkSettlement {
  settlementId Int @id @default(autoincrement()) @map("settlement_id")

  employeeId Int

  // ----------------------------------------------------
  // OPTIONAL PAYROLL REFERENCE
  // ----------------------------------------------------

  // A settlement may optionally be associated with the
  // payroll period that generated the overtime.
  //
  // It is NOT required because variable payment is an
  // independent payment event.
  //
  // It is also NOT unique because multiple settlement
  // records can exist for the same payroll period.

  payrollId Int? @map("payroll_id")

  // Total accumulated extra hours settled.
  settledHours Decimal @map("settled_hours") @db.Decimal(8, 2)

  // Incentive amount decided by admin.
  incentiveAmount Decimal @map("incentive_amount") @db.Decimal(12, 2)

  // Explicitly supplied by the application when settlement occurs.
  //
  // No database CURRENT_TIMESTAMP default is used here because
  // MySQL can reject CURRENT_TIMESTAMP as a DATE default.
  settlementDate DateTime @map("settlement_date") @db.Date

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt

  employee Employee @relation(fields: [employeeId], references: [employeeId])

  // Optional because variable payment does not require
  // normal payroll to be paid.
  payroll Payroll? @relation(fields: [payrollId], references: [payrollId])

  // All ExtraWork records included in this settlement.
  extraWorkRecords ExtraWork[]

  @@index([employeeId])
  @@index([payrollId])
  @@index([settlementDate])
  @@map("extra_work_settlements")
}

// ======================================================
// PUBLIC HOLIDAY
// ======================================================
//
// Public holidays are branch-specific.
//
// One database record represents one branch holiday
// on one specific date.
//
// For a multi-day holiday, the application creates
// one PublicHoliday record for each date.
//
// Example:
//
// 2026-10-02 -> Gandhi Jayanti
// 2026-10-03 -> Company Holiday
//
// This allows individual dates to be edited or deleted.
//
// Paid holiday:
//   isPaid = true
//   dailyWorkingHours contributes to payroll.
//
// Unpaid holiday:
//   isPaid = false
//   contributes 0 hours to payroll.
//
// The holiday does NOT create an Attendance record.
// Fingerprint attendance remains actual attendance.
// ======================================================

model PublicHoliday {
  publicHolidayId Int @id @default(autoincrement()) @map("public_holiday_id")

  // Company that owns this public holiday.
  companyId Int

  // Branch to which this public holiday applies.
  branchId Int

  // Specific holiday date.
  holidayDate DateTime @map("holiday_date") @db.Date

  // Display name of the public holiday.
  holidayName String @map("holiday_name") @db.VarChar(150)

  // Working hours credited for a paid public holiday.
  //
  // Example:
  // 8.00 = eight paid hours
  // 7.50 = seven and a half paid hours
  dailyWorkingHours Decimal @default(0.00) @map("daily_working_hours") @db.Decimal(5, 2)

  // true  = paid public holiday
  // false = unpaid public holiday
  isPaid Boolean @default(true) @map("is_paid")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  company companies @relation(fields: [companyId], references: [company_id])
  branch  Branch    @relation(fields: [branchId], references: [branchId])

  // A branch cannot have two public-holiday records
  // for the same date.
  @@unique([branchId, holidayDate])
  @@index([companyId])
  @@index([branchId])
  @@index([holidayDate])
  @@map("public_holidays")
}

// ======================================================
// ACTIVITY LOG
// ======================================================

model ActivityLog {
  logId       BigInt   @id @default(autoincrement()) @map("log_id")
  employeeId  Int?
  actionType  String   @map("action_type") @db.VarChar(50)
  description String?  @db.Text
  createdAt   DateTime @default(now()) @map("created_at")

  employee Employee? @relation(fields: [employeeId], references: [employeeId])

  @@index([employeeId])
  @@index([actionType])
  @@index([createdAt])
  @@map("activity_logs")
}

// ======================================================
// SETTINGS
// ======================================================

model Setting {
  settingId Int      @id @default(autoincrement()) @map("setting_id")
  companyId Int
  key       String   @db.VarChar(100)
  value     String?  @db.Text
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  company companies @relation(fields: [companyId], references: [company_id])

  @@unique([companyId, key])
  @@index([companyId])
  @@map("settings")
}

// ======================================================
// COMPANY
// ======================================================

model companies {
  company_id    Int      @id @default(autoincrement())
  company_name  String   @db.VarChar(150)
  address       String?  @db.Text
  contact_email String?  @db.VarChar(150)
  contact_phone String?  @db.VarChar(20)
  created_at    DateTime @default(now())
  updated_at    DateTime

  admins         Admin[]
  branches       Branch[]
  departments    Department[]
  employees      Employee[]
  devices        IotDevice[]
  leaveTypes     LeaveType[]
  settings       Setting[]
  publicHolidays PublicHoliday[]
}

// ======================================================
// LEAVE TYPE
// ======================================================

model LeaveType {
  leaveTypeId       Int      @id @default(autoincrement()) @map("leave_type_id")
  companyId         Int      @map("company_id")
  name              String   @db.VarChar(100)
  code              String   @db.VarChar(30)
  description       String?  @db.Text
  annualQuota       Decimal? @map("annual_quota") @db.Decimal(6, 2)
  isPaid            Boolean  @default(true) @map("is_paid")
  requiresApproval  Boolean  @default(true) @map("requires_approval")
  allowHalfDay      Boolean  @default(true) @map("allow_half_day")
  allowCarryForward Boolean  @default(false) @map("allow_carry_forward")
  status            String   @default("ACTIVE") @db.VarChar(20)
  createdAt         DateTime @default(now()) @map("created_at")
  updatedAt         DateTime @updatedAt @map("updated_at")

  balances LeaveBalance[]
  requests LeaveRequest[]
  company  companies      @relation(fields: [companyId], references: [company_id])

  @@unique([companyId, code])
  @@unique([companyId, name])
  @@index([companyId])
  @@index([status])
  @@map("leave_types")
}

// ======================================================
// LEAVE BALANCE
// ======================================================

model LeaveBalance {
  leaveBalanceId Int      @id @default(autoincrement()) @map("leave_balance_id")
  employeeId     Int      @map("employee_id")
  leaveTypeId    Int      @map("leave_type_id")
  year           Int
  allocated      Decimal  @default(0.00) @db.Decimal(6, 2)
  used           Decimal  @default(0.00) @db.Decimal(6, 2)
  remaining      Decimal  @default(0.00) @db.Decimal(6, 2)
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")

  employee  Employee  @relation(fields: [employeeId], references: [employeeId])
  leaveType LeaveType @relation(fields: [leaveTypeId], references: [leaveTypeId])

  @@unique([employeeId, leaveTypeId, year])
  @@index([employeeId])
  @@index([leaveTypeId])
  @@index([year])
  @@map("leave_balances")
}

// ======================================================
// LEAVE REQUEST
// ======================================================

model LeaveRequest {
  leaveRequestId Int @id @default(autoincrement()) @map("leave_request_id")
  employeeId     Int @map("employee_id")
  leaveTypeId    Int @map("leave_type_id")

  // Original requested leave dates
  startDate DateTime @map("start_date") @db.Date
  endDate   DateTime @map("end_date") @db.Date
  totalDays Decimal  @map("total_days") @db.Decimal(6, 2)

  // Employee reason
  reason String? @db.Text

  // Request status
  status String @default("PENDING") @db.VarChar(20)

  // Approval information
  approvedBy Int?      @map("approved_by")
  approvedAt DateTime? @map("approved_at")

  // Approved leave period
  approvedStartDate DateTime? @map("approved_start_date") @db.Date
  approvedEndDate   DateTime? @map("approved_end_date") @db.Date
  approvedDays      Decimal?  @map("approved_days") @db.Decimal(6, 2)

  // Rejection information
  rejectionReason String? @map("rejection_reason") @db.Text

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  employee  Employee  @relation(fields: [employeeId], references: [employeeId])
  leaveType LeaveType @relation(fields: [leaveTypeId], references: [leaveTypeId])

  @@index([employeeId])
  @@index([leaveTypeId])
  @@index([status])
  @@index([startDate, endDate])
  @@index([approvedStartDate, approvedEndDate])
  @@map("leave_requests")
}

// ======================================================
// FINGERPRINT ENROLLMENT
// ======================================================

model FingerprintEnrollment {
  enrollmentId Int       @id @default(autoincrement()) @map("enrollment_id")
  employeeId   Int       @map("employee_id")
  sensorSlot   Int       @map("sensor_slot")
  fingerName   String?   @map("finger_name") @db.VarChar(30)
  status       String    @default("PENDING") @db.VarChar(20)
  confidence   Int?
  errorMessage String?   @map("error_message") @db.Text
  completedAt  DateTime? @map("completed_at")
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @updatedAt @map("updated_at")

  employee Employee @relation(fields: [employeeId], references: [employeeId])

  @@index([employeeId])
  @@index([status])
  @@index([sensorSlot])
  @@index([createdAt])
  @@map("fingerprint_enrollments")
}
